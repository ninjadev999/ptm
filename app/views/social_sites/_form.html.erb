<%= form_with(model: social_site, local: true) do |form| %>
  <% if social_site.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(social_site.errors.count, "error") %> prohibited this social_site from being saved:</h2>

      <ul>
      <% social_site.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :image_url %>
    <%= form.text_field :image_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

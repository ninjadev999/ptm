<%= form_with(model: public_site, local: true) do |form| %>
  <% if public_site.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(public_site.errors.count, "error") %> prohibited this public_site from being saved:</h2>

      <ul>
      <% public_site.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="field">
    <%= form.label :image_url %>
    <%= form.text_field :image_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
